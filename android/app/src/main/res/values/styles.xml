<resources>

    <!-- Single AppTheme (merged correctly) -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- ANDROID 12+ SYSTEM SPLASH SCREEN -->
        <item name="android:windowSplashScreenBackground">@color/grey_50</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenAnimationDuration">5900</item>

    </style>

</resources>
